import React from "react";

export default function UserFile({ owner_id, owner_name, original_filename, filename }) {
    return (
        <div id='file-info-container'>
            <b> owner_name </b> original_filename;
        </div>
    );
}